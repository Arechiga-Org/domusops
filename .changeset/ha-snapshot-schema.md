---
"@domusops/schema": minor
---

Add the `domusops.snapshot/0.1` format: types, the omission list, field defaults, the redaction marker, the key-space helpers, a JSON Schema, and a reference decoder (`expand`). Replaces the placeholder `HaSnapshot` type, which is a breaking change in 0.x.
